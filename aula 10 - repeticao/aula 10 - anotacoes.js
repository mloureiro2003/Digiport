/*
for (inicio; condição; incremento)
    - controle total sobre o índice
    - ex: for (int i = 0; i < 5; i++)
    - que nem o C
        - for (int i = 0; i < 5; i++)
            console.log (`${i} segundos`);

for elemento in coleção
    - acessar cada item diretamente
    - pega o ELEMENTO não o ÍNDICE
    - ex: for nome in nomes;
    - que nem o python versão 2
        for monster in monsters
*/